[package]
name = "rust_fuse"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_fuse"
crate-type = ["cdylib"]

[build-dependencies]
cc = "1.0"
# 可选依赖
log = "0.4"
serde = { version = "1.0", features = ["derive"] }

[dependencies]
libc = "0.2"